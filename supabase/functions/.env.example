# Supabase automatically provisions its URL, publishable keys, secret keys, and
# JWKS inside hosted and local Edge Functions. No custom secret is required.
#
# Never copy SUPABASE_SECRET_KEYS, SUPABASE_SECRET_KEY, or the legacy
# SUPABASE_SERVICE_ROLE_KEY into Vite/Vercel browser environment variables.

